from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A chrome plugin to analyse the comments on a youtube video like sentiment analysis',
    author='Gandharv',
    license='',
)
